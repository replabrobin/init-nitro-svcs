#!/bin/sh
set -e

! pgrep --quiet elogind-daemon && ! pgrep --quiet elogind && exit 1 || true

exec 2>&1

[ -d /run/lightdm ] || install -dm0711 -olightdm -glightdm /run/lightdm

exec /usr/bin/lightdm
